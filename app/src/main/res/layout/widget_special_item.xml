<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_special_item_content_iv"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:scaleType="fitXY"/>

    <ImageView
        android:id="@+id/widget_special_item_play_iv"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="@drawable/btn_detail_play_press"
        android:layout_marginRight="10dp"
        android:layout_marginLeft="20dp"
        android:layout_marginBottom="10dp"
        android:layout_alignBottom="@+id/widget_special_item_content_iv"
        android:layout_alignRight="@+id/widget_special_item_content_iv"
        android:layout_alignEnd="@+id/widget_special_item_content_iv"/>

    <TextView
        android:id="@+id/special_item_des_tv"
        android:layout_width="70dp"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:singleLine="true"
        android:ellipsize="end"
        android:text="item"
        android:textColor="#fff"
        android:layout_alignBottom="@+id/widget_special_item_content_iv"
        android:layout_alignLeft="@+id/widget_special_item_content_iv"/>

    <TextView
        android:id="@+id/widget_special_item_name_tv"
        android:layout_width="150dp"
        android:maxLines="2"
        android:layout_below="@+id/widget_special_item_content_iv"
        android:layout_marginTop="1dp"
        android:layout_height="wrap_content"
        android:text="des"/>
</RelativeLayout>